# Design-system governance

## Rules of thumb

- reuse existing primitives before adding new component families
- keep tokens and themes centralized
- treat accessibility regressions as release-blocking for user-facing flows
- when redesigning intentionally, update design-system docs first so later work does not drift
