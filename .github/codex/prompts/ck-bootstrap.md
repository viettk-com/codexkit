<!-- generated by scripts/render-command-palette.py -->
# bootstrap

Equivalent quick commands: `/ck:bootstrap`, `/ck:scan`.
Equivalent skill shorthands: `$ck-bootstrap`, `$ck-scan`.

Use the `command-router` alias logic, then route to these canonical skills:
- $bootstrap

Related agents:
- bootstrap_curator

Related scripts:
- python3 scripts/bootstrap-codexkit.py --apply

Typical artifacts touched:
- docs/project-context/index.md
- AGENTS.md managed bootstrap block

Instructions:
- preserve any payload after the alias and pass it into the resolved workflow
- read `docs/project-context/index.md` and `AGENTS.md` before risky work
- keep the result architecture-safe and proportional to the change class
- note: Run this first in unfamiliar repositories or after major structural changes.

Output format:
# Resolved command
## Canonical workflow
## Next artifact or command
## Key risks or assumptions
